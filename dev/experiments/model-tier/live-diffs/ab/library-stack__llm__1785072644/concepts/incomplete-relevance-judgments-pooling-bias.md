## Concept: Incomplete Gold-Standard Relevance Judgments and Pooling Bias in Retrieval Evaluation

slug: incomplete-relevance-judgments-pooling-bias
title: Incomplete Gold-Standard Relevance Judgments and Pooling Bias in Retrieval Evaluation
source_paths:
  - sources/incoming/llms-patch-missing-relevance-judgments.md (tier 2)
target_article: 

### Claims

- The Cranfield paradigm originally assumed relevance judgments must be complete — every document in the corpus reviewed for every query — but as collection size grew this completeness assumption became "increasingly impossible to maintain," making ground-truth relevance judgments "inevitably incomplete in IR test collections" at scale (citing Buckley and Voorhees, 2004). [source: llms-patch-missing-relevance-judgments, line ~12]
- **Pooling** is the standard mitigation: only the top results from one or more retrieval systems are selected for manual judgment, rather than the whole corpus. Pooling's reliability "heavily rests on the depth of the selected documents, and the choice of retrieval systems." [source: llms-patch-missing-relevance-judgments, line ~12]
- Pooling introduces its own artifacts/bias: "if the retrieval systems leveraged for relevance judgment were all based on BM25, chances are test models that use BM25 score higher in the test collection." Diversifying contributing systems and judging deeper reduces this risk but costs more manual labor and slower judgment turnaround. [source: llms-patch-missing-relevance-judgments, line ~12]
- Unjudged documents ("holes") are handled by standard IR metrics (nDCG@k, MAP, Pr@k) by either ignoring them or treating them as non-relevant — both approaches "miss the true essence of the complete judgment" and can miss relevant-but-unjudged documents, yielding no measured gain for genuinely relevant results the annotator never saw. This is exactly the failure mode of a hand-annotated gold set that is under-inclusive: a defensible answer absent from the reference set scores as a false positive/non-relevant rather than as correct. [source: llms-patch-missing-relevance-judgments, line ~12]
- Different retrieval models/systems have different degrees of sparsity in their judgments (i.e., they return different volumes of unjudged items), which "renders their comparison unfair, particularly for models returning many unjudged documents" — cross-model comparison against a shared incomplete gold set is not apples-to-apples when systems differ in how many out-of-pool items they surface. [source: llms-patch-missing-relevance-judgments, line ~12]
- These shortcomings collectively produce "an inaccurate assessment of retrieval models, giving us a misleading sense of progress" — i.e., measured precision/effectiveness against an incomplete reference set is a lower bound on true effectiveness, not an accurate estimate. [source: llms-patch-missing-relevance-judgments, line ~12]
- The paper's proposed fix: use an LLM as an automated relevance assessor to fill holes in the judgment set, instructed with detailed guidelines to assign fine-grained (not just binary) TREC-style relevance labels (0 = nothing to do with the query, 1 = related but doesn't answer, 2 = has some answer but unclear/buried, 3 = dedicated answer) rather than the binary relevant/non-relevant labels used in prior LLM-assessor work. [source: llms-patch-missing-relevance-judgments, line ~12]
- Method: simulate varying degrees of holes by randomly dropping relevant documents from existing gold judgments on TREC DL datasets, then run the LLM assessor to patch the holes and compare its labels against the original (ground-truth) judgments. [source: llms-patch-missing-relevance-judgments, line ~12]
- Result: the LLM-assessor labels correlate strongly with ground-truth relevance judgments even under extreme hole rates — in the scenario retaining only 10% of original judgments, the method achieves a Kendall τ correlation of 0.87 (Vicuña-7B, open-source) and 0.92 (GPT-3.5 Turbo, proprietary) averaged across three TREC DL datasets. This suggests LLM-filled holes are a viable way to de-bias an under-inclusive gold set without full re-annotation. [source: llms-patch-missing-relevance-judgments, line ~12]
- Implication for practitioners with an internal, hand-annotated extraction/retrieval benchmark: if the gold set was built by pooling (or by any partial manual review) and is under-inclusive, precision/accuracy measured against it should be read as a lower bound, not the true score, and the "false positives" flagged against the gold set may simply be unjudged-but-correct items; an LLM-assessor pass to fill the gaps (with fine-grained rather than binary labels) is a validated way to recover a more accurate effectiveness estimate without redoing full manual annotation. [source: llms-patch-missing-relevance-judgments, line ~12]

### Notes

- Distinct from `llm-as-judge` (judging comparative quality/correctness of LLM-generated *outputs*) and from `production-eval-systems` (golden-dataset curation for production regression testing): this concept is specifically about relevance-judgment *completeness* in retrieval/extraction reference sets — the pooling process, why gaps are structural not accidental, and using an LLM to fill those specific gaps. No existing article in the scope map covers pooling/incomplete-reference-set bias, so a new slug was minted per the task brief's own read.
- Source is an arXiv preprint (Upadhyay, Kamalloo, Lin — University of Waterloo, 2024-05-08), rated tier 2 (Standard — arxiv paper) per STACK.md's hierarchy.
- The fetched source text truncates mid-prompt-template in the Methodology section (cuts off after "Instructions:"); the Experimental Setup, full Results tables, and Data Contamination Testing sections were not present in the text available to extract from. All claims above are drawn from the Abstract, Introduction, and Related Work sections, which were fully present.

